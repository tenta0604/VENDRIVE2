# Workflow hygiene

Canonical reusable workflows after the 2026-09-16 cleanup are `create-safety-tag.yml`, `an15-hardening-tests.yml`, and `maintenance-executor.yml`. Historical `ops10a-*` and `ops10b2-exec.yml` files are retired one-off executors and must not be restored unless their original phase is deliberately reopened.
