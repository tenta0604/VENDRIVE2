name: AN14B3 Gateway Diagnostic

on:
  push:
    branches:
      - chatgpt/an14b3-work-20260914

permissions:
  contents: read

jobs:
  gateway-diagnostic:
    runs-on: ubuntu-latest
    steps:
      - name: Check Vercel AI Gateway auth/model path
        shell: bash
        run: |
          set -euo pipefail
          sleep 5
          curl -sS -X POST \
            -H 'Origin: https://tenta0604.github.io' \
            https://vendrive2-ocr-relay.vercel.app/api/gateway-check

# Git connection trigger: 2026-09-14T14:28+09:00
