import {deflateSync} from "node:zlib";
function crc32(buf){let c=0xffffffff;for(const b of buf){c^=b;for(let k=0;k<8;k++)c=(c>>>1)^((c&1)?0xedb88320:0)}return (c^0xffffffff)>>>0}
function chunk(type,data){const t=Buffer.from(type),out=Buffer.alloc(12+data.length);out.writeUInt32BE(data.length,0);t.copy(out,4);data.copy(out,8);out.writeUInt32BE(crc32(Buffer.concat([t,data])),8+data.length);return out}
function image(width=1200,height=1800){const stride=width+1,raw=Buffer.alloc(stride*height);let seed=123456;for(let y=0;y<height;y++){const row=y*stride;raw[row]=0;for(let x=0;x<width;x++){seed=(Math.imul(seed,1664525)+1013904223)>>>0;const paper=x>180&&x<1020&&y>70&&y<1730,v=paper?(220+(seed%30)):(20+(seed%45));raw[row+1+x]=v}}const h=Buffer.alloc(13);h.writeUInt32BE(width,0);h.writeUInt32BE(height,4);h[8]=8;h[9]=0;return Buffer.concat([Buffer.from([137,80,78,71,13,10,26,10]),chunk("IHDR",h),chunk("IDAT",deflateSync(raw,{level:0})),chunk("IEND",Buffer.alloc(0))])}
const endpoint="https://vendrive2-ocr-relay.vercel.app/api/ocr",png=image();
const form=new FormData();form.append("file",new Blob([png],{type:"image/png"}),"receipt-like.png");form.append("supportedReportTypes",JSON.stringify(["sales","input","recovery","input_recovery"]));
const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),80000),started=Date.now();
try{const response=await fetch(endpoint,{method:"POST",headers:{Origin:"https://tenta0604.github.io",Accept:"application/json"},body:form,signal:controller.signal});const text=await response.text();let body=null;try{body=JSON.parse(text)}catch{};console.log("OCR_UX_PRODUCTION_SMOKE",JSON.stringify({bytes:png.length,status:response.status,elapsedMs:Date.now()-started,provider:body&&body.provider||null,code:body&&body.code||null}));if(response.status!==200||!body||body.provider!=="google-gemini-api")throw new Error("production OCR smoke failed")}finally{clearTimeout(timer)}
